### **Level 1: Basic Form Structure**

First, we'll lay down the raw HTML for the form on the contact page.

**ToDo**

1. Open the `contact.html` file.  
2. Inside the `<main>` section, add a `<form>` element containing two `<input>` fields and one `<button>`.

### **Level 2: Semantic and Accessible Form**

Now, we'll make the form accessible by properly associating labels with their corresponding inputs. This is a critical step for screen readers and improves usability for everyone.

**ToDo**

1. Add a `<label>` for each `<input>` and `<textarea>`.  
2. Connect each label to its control using the `for` and `id` attributes.  
3. **Solution:** Update the form in `contact.html` to include the labels and attributes.

### **Level 3: Styling the Form**

Let's apply the portfolio's theme to the form and use the box model to create a clean, readable layout.

**ToDo**

1. Add CSS rules to style the `input`, `textarea`, and `button` elements to match your site's fonts and colors.  
2. Use `padding` and `margin` to space out the form elements properly.

### **Level 4: Interactive Feedback**

Let's give the user clear visual cues when they interact with the form by using pseudo-classes.

**ToDo**

1. Add a `:hover` style to the submit button.  
2. Add a `:focus` style to the text inputs and the `textarea`.  
3. Commit all the form changes to Git.

### **Level 5: Polished Transitions**

Finally, let's make the interactive feedback smooth and professional by applying CSS transitions.

**ToDo**

1. Add the `transition` property to the `input`, `textarea`, and `button` elements.
